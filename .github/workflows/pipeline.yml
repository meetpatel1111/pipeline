on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  script:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: |
          pwd
          cd ..
          ls
          echo "Hello"
